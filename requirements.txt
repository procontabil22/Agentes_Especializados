# ── LangChain ────────────────────────────────────────────────────────────────
langchain==0.3.7
langchain-community==0.3.7
langchain-openai==0.2.7
langchain-anthropic==0.3.0
langchain-text-splitters==0.3.2

# ── Docling (PDF → Markdown estruturado com preservação de layout/tabelas) ───
docling==2.5.1
docling-core==2.5.0

# ── Supabase + pgvector ──────────────────────────────────────────────────────
supabase==2.10.0

# ── Google Drive API ─────────────────────────────────────────────────────────
google-api-python-client==2.151.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# ── OpenAI (embeddings text-embedding-3-small) ───────────────────────────────
openai==1.54.3
tiktoken==0.8.0

# ── Crawler web (portais JSF/SPA + estáticos) ────────────────────────────────
playwright==1.48.0          # renderiza portais JavaScript/JSF (SEFAZ-MA, etc.)
beautifulsoup4==4.12.3      # parsing HTML auxiliar

# ── FastAPI + servidor ───────────────────────────────────────────────────────
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# ── Agendamento de tarefas ───────────────────────────────────────────────────
apscheduler==3.10.4

# ── Utilitários ──────────────────────────────────────────────────────────────
httpx==0.27.2
requests==2.32.3
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0
loguru==0.7.2
